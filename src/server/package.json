{
  "name": "omni-extension-sharp",
  "packageManager": "yarn@3.3.1",
  "scripts": {
    "build": "esbuild --bundle ./extension.ts --outdir=../../server  --format=esm --analyze --color=true --target=es2020 --platform=node --external:'axios'"
  },
  "devDependencies": {
    "@types/tar-fs": "^2.0.1",
    "esbuild": "^0.18.4",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
